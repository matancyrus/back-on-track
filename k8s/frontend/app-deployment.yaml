apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-on-track-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: back-on-track-frontend
  template:
    metadata:
      labels:
        app: back-on-track-frontend
    spec:
      containers:
        - name: back-on-track-frontend
          image: matancyrus/back-on-track-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          volumeMounts:
          - name: frontend
            mountPath: /etc/nginx/conf.d/default.conf
            subPath: nginx.conf
            readOnly: false
      volumes:
      - name: frontend
        configMap:
          name: frontend-config